     -*-*-*-* REMEMBER ME *-*-*-*-
=============================================
* Things I don't want to have to find again *
=============================================
		OR:
====================================
* Throwaway code I want to recycle *
====================================

) freebsd-zfs-root-enc/*
  Python Fabric based FreeBSD automated install.
  TODO: Make this a shell script or something.

) copy_add_prefix.mysql - MySQL Stored proc to duplicate
  tables in a DB and add a prefix to each table

) ./strace_apache.sh -f -v -o strace.out

) freebsd-csh-ssh-agent
  Bits to add to cshrc to manage ssh-agent on shell start.
  TODO: Add logic to key mgmt (ie: lock a key after X hours)
